NetworkTransport.h (Abstract Base Class definining the transport interface)
WebSocketTransport.h (Public API and class definitions)
HttpTransport.h (Public API and class definitions)

the provider should hold the std::thread oithread that the IOC will run on as well as create the ioc that way it is hidden from end users

just give me the header files, and explain what else I have to change
